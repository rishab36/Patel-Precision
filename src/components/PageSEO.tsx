"use client";
// Next.js handles SEO via app/<route>/page.tsx `metadata` exports.
// This component is preserved as a no-op for backward compatibility.
type Props = Record<string, any>;
const PageSEO = (_props: Props) => null;
export default PageSEO;
